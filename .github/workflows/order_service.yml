name: Deploy Order Service

on:
  push:
    branches:
      - main
    paths:
      - 'terraform/deployment/order_service/**'
      - '.github/workflows/order_service.yml'
      - 'order_service/**'

permissions:
  contents: read
  id-token: write

jobs:
  deploy:
    runs-on: ubuntu-latest

    defaults:
        run:
            working-directory: terraform/deployment/order_service
        

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v3

      

      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v2
        with:
          role-to-assume: ${{ secrets.AWS_ROLE_ARN }}
          aws-region: eu-north-1

      - name: Set up terraform
        uses: hashicorp/setup-terraform@v1
        with:
          terraform_version: '1.5.0'

    
      - name: Terraform Init
        run: terraform init
        

      - name: Terraform Plan
        run: terraform plan -var-file="dev.tfvars"

      # - name: Terraform Apply
      #   run: terraform apply -var-file="dev.tfvars" -auto-approve

      # - name: Login to Amazon ECR
      #   uses: aws-actions/amazon-ecr-login@v1

      

      # - name: Build and Push Order Service Image to ECR
      #   run: |
      #     docker buildx build --platform linux/amd64 -t order-service ../../../order_service 
      #     docker tag order-service ${{ secrets.ECR_REGISTRY }}/order-service:latest
      #     docker push ${{ secrets.ECR_REGISTRY }}/order-service:latest

      

      # - name: Update ECS Service
      #   run: |          
      #     aws ecs update-service --cluster microservices-app --service order-service --force-new-deployment
          

      # - name: Trivy Scan
      #   uses: aquasecurity/trivy-action@master
      #   with:
      #     image-ref: ${{ secrets.ECR_REGISTRY }}/order-service:latest
      #     format: json
      #     output: 'trivy-report-order-service.json'
      #     exit-code: 0
      #     severity: 'CRITICAL,HIGH'
      #     ignore-unfixed: true

      # - name: Upload security reports
      #   uses: actions/upload-artifact@v4
      #   with:
      #     name: security-reports
      #     path: |
      #       trivy-report-order-service.json

      - name: Terraform destroy
        run: terraform destroy -var-file="dev.tfvars" -auto-approve